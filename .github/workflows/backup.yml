name: Back up simplifier source code

on:
  workflow_dispatch:
  push:
  schedule:
    - cron: '0 0 * * *'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          token: ${{ secrets.TOKEN }}
      - name: Download and commit simplifier source code
        run: |
          wget https://simplifier.net/ukdigitalmedicine/download; \
          export BRNAME=$(date | tr -d "[:space:]" | tr -d ":");
      - name: Commit report
        run: |
          git config --global user.name 'Declan Kieran'
          git config --global user.email 'declan.kieran1@nhs.net'
          git checkout -b $BRNAME
          git add -A
          git commit -m "$(date)"
          git push
          
      - name: Create Pull Request
        uses: actions/github-script@v6
        with:
          github_token: ${{ secrets.SIMPLIFIERSRCREPO }}
          script: |
            export TODAY=$(date)
            const { repo, owner } = context.repo;
            const result = await github.rest.pulls.create({
              title: $TODAY,
              owner,
              repo,
              head: '${{ github.ref_name }}',
              base: 'main',
              body: [
                'This PR is auto-generated by',
                '[actions/github-script](https://github.com/actions/github-script).'
              ].join('\n')
            });
            github.rest.issues.addLabels({
              owner,
              repo,
              issue_number: result.data.number,
              labels: ['feature', 'automated pr']
            });

      - name: "Merge pull request"
        uses: "actions/github-script@v6"
        with:
          github_token: ${{ secrets.SIMPLIFIERSRCREPO }}
          script: |
            const pullRequest = context.payload.workflow_run.pull_requests[0]
            const repository = context.repo

            await github.rest.pulls.merge({
              merge_method: "merge",
              owner: repository.owner,
              pull_number: pullRequest.number,
              repo: repository.repo,
            })